import React from "react";
import Loader from "../atoms/Loader";
import SnippetList from "../organisms/SnippetList";

const Home = () => {
  return (
    <main>
      <SnippetList />
    </main>
  );
};

export default Home;
